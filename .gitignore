# Secrets and per-deployment configuration — never committed.
# This repository is public; keep it that way by keeping these out.
.env
acl.json

# Rendered nginx route: contains the real API keys.
# Generated from acl.json by render-route.sh, which is what is versioned.
todoist.conf

# Runtime
logs/
node_modules/
bun.lockb
*.log
